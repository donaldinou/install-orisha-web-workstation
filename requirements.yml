---
collections:
  # Pinned to a range compatible with the ansible-core version shipped by the
  # official Ansible PPA (currently core 2.17). Newer community.general (13.x)
  # requires a more recent core and would fail to load.
  - name: community.general
    version: ">=10.0.0,<11.0.0"
